apiVersion: v1
kind: ConfigMap
metadata:
  namespace: {{ metallb_namespace }}
  name: config
data:
  config: |
    address-pools:
    - name: default
      protocol: layer2
      addresses:
      - {{ metallb_ip_range }}
